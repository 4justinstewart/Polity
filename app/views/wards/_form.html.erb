<%= form_for(@ward) do |f| %>
  <% if @ward.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ward.errors.count, "error") %> prohibited this ward from being saved:</h2>

      <ul>
      <% @ward.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ward_number %><br>
    <%= f.number_field :ward_number %>
  </div>
  <div class="field">
    <%= f.label :term_start %><br>
    <%= f.date_select :term_start %>
  </div>
  <div class="field">
    <%= f.label :term_end %><br>
    <%= f.date_select :term_end %>
  </div>
  <div class="field">
    <%= f.label :legislator_id %><br>
    <%= f.text_field :legislator_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
